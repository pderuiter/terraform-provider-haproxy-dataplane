---
page_title: "haproxy-dataplane_group Resource - HAProxy Data Plane"
subcategory: "Configuration"
description: |-
  Manages HAProxy group configuration.
---

# haproxy-dataplane_group (Resource)

Use this resource to manage the group object in HAProxy Data Plane API.

## Example Usage

### Minimal declaration

Use this pattern when you want a concise resource declaration with only required fields.

```terraform
resource "haproxy-dataplane_group" "example" {
  name     = "example_group"
  userlist = "example"

  # Replace with required fields for this object in your environment.
  spec = {}
}
```

### Operational module pattern

Use this pattern when exposing a reusable module interface for teams. It adds variables, a stable naming pattern, and an output.

```terraform
locals {
  group_name = "managed_group"
}

variable "userlist" {
  type = string
}

resource "haproxy-dataplane_group" "managed" {
  name     = local.group_name
  userlist = var.userlist

  # Replace with required fields for this object in your environment.
  spec = {}
}

output "group_id" {
  value = haproxy-dataplane_group.managed.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String)
- `spec` (Attributes) (see [below for nested schema](#nestedatt--spec))
- `userlist` (String)

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedatt--spec"></a>
### Nested Schema for `spec`

Optional:

- `metadata` (Dynamic)
- `users` (String)
