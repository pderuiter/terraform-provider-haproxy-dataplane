---
page_title: "haproxy-dataplane_http_errors_section Resource - HAProxy Data Plane"
subcategory: "Configuration"
description: |-
  Manages HAProxy http errors section configuration.
---

# haproxy-dataplane_http_errors_section (Resource)

Use this resource to manage the http_errors_section object in HAProxy Data Plane API.

## Example Usage

### Minimal declaration

Use this pattern when you want a concise resource declaration with only required fields.

```terraform
resource "haproxy-dataplane_http_errors_section" "example" {
  name = "example_http_errors_section"

  # Replace with required fields for this object in your environment.
  spec = {}
}
```

### Operational module pattern

Use this pattern when exposing a reusable module interface for teams. It adds variables, a stable naming pattern, and an output.

```terraform
locals {
  http_errors_section_name = "managed_http_errors_section"
}

resource "haproxy-dataplane_http_errors_section" "managed" {
  name = local.http_errors_section_name

  # Replace with required fields for this object in your environment.
  spec = {}
}

output "http_errors_section_id" {
  value = haproxy-dataplane_http_errors_section.managed.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String)
- `spec` (Attributes) (see [below for nested schema](#nestedatt--spec))

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedatt--spec"></a>
### Nested Schema for `spec`

Required:

- `error_files` (Attributes List) (see [below for nested schema](#nestedatt--spec--error_files))

Optional:

- `metadata` (Dynamic)

<a id="nestedatt--spec--error_files"></a>
### Nested Schema for `spec.error_files`

Optional:

- `code` (Number)
- `file` (String)
