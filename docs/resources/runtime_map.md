---
page_title: "haproxy-dataplane_runtime_map Resource - HAProxy Data Plane"
subcategory: "Configuration"
description: |-
  Manages HAProxy runtime map configuration.
---

# haproxy-dataplane_runtime_map (Resource)

Use this resource to manage the runtime_map object in HAProxy Data Plane API.

## Example Usage

### Minimal declaration

Use this pattern when you want a concise resource declaration with only required fields.

```terraform
resource "haproxy-dataplane_runtime_map" "example" {
  name = "example_runtime_map"

  # Replace with required fields for this object in your environment.
  spec = {}
}
```

### Operational module pattern

Use this pattern when exposing a reusable module interface for teams. It adds variables, a stable naming pattern, and an output.

```terraform
locals {
  runtime_map_name = "managed_runtime_map"
}

resource "haproxy-dataplane_runtime_map" "managed" {
  name = local.runtime_map_name

  # Replace with required fields for this object in your environment.
  spec = {}
}

output "runtime_map_id" {
  value = haproxy-dataplane_runtime_map.managed.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String)
- `spec` (Dynamic)

### Read-Only

- `id` (String) The ID of this resource.
