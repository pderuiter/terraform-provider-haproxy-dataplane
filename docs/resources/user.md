---
page_title: "haproxy-dataplane_user Resource - HAProxy Data Plane"
subcategory: "Configuration"
description: |-
  Manages HAProxy user configuration.
---

# haproxy-dataplane_user (Resource)

Use this resource to manage the user object in HAProxy Data Plane API.

## Example Usage

This baseline example shows the minimum Terraform shape for this resource.
Use it as a starting point and adjust the required fields to match your HAProxy configuration model.

```terraform
resource "haproxy-dataplane_user" "example" {
  userlist = "example"
  username = "example"

  # Replace with required fields for this object in your environment.
  spec = {}
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `spec` (Attributes) (see [below for nested schema](#nestedatt--spec))
- `userlist` (String)
- `username` (String)

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedatt--spec"></a>
### Nested Schema for `spec`

Required:

- `password` (String)
- `secure_password` (Boolean)

Optional:

- `groups` (String)
- `metadata` (Dynamic)
