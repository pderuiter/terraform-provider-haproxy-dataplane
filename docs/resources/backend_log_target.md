---
page_title: "haproxy-dataplane_backend_log_target Resource - HAProxy Data Plane"
subcategory: "Configuration"
description: |-
  Manages HAProxy backend log target configuration.
---

# haproxy-dataplane_backend_log_target (Resource)

Use this resource to manage the backend_log_target object in HAProxy Data Plane API.

## Example Usage

### Minimal declaration

Use this pattern when you want a concise resource declaration with only required fields.

```terraform
resource "haproxy-dataplane_backend_log_target" "example" {
  parent_name = "example_parent"

  # Replace with required fields for this object in your environment.
  spec = {}
}
```

### Operational module pattern

Use this pattern when exposing a reusable module interface for teams. It adds variables, a stable naming pattern, and an output.

```terraform
locals {
  backend_log_target_name = "managed_backend_log_target"
}

variable "parent_name" {
  description = "Parent object name, for example a frontend or backend name."
  type        = string
}

resource "haproxy-dataplane_backend_log_target" "managed" {
  parent_name = var.parent_name

  # Replace with required fields for this object in your environment.
  spec = {}
}

output "backend_log_target_id" {
  value = haproxy-dataplane_backend_log_target.managed.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `parent_name` (String)
- `spec` (Dynamic)

### Read-Only

- `id` (String) The ID of this resource.
