resource "haproxy-dataplane_peer_entry" "example" {
  name         = "example_peer_entry"
  peer_section = "example"

  # Replace with required fields for this object in your environment.
  spec = {}
}
