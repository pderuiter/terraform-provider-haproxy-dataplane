---
page_title: "haproxy-dataplane_default_tcp_check Resource - HAProxy Data Plane"
subcategory: "Configuration"
description: |-
  Manages HAProxy default tcp check configuration.
---

# haproxy-dataplane_default_tcp_check (Resource)

Use this resource to manage the default_tcp_check object in HAProxy Data Plane API.

## Example Usage

### Minimal declaration

Use this pattern when you want a concise resource declaration with only required fields.

{{tffile "examples/resources/default_tcp_check/example_1.tf"}}

### Operational module pattern

Use this pattern when exposing a reusable module interface for teams. It adds variables, a stable naming pattern, and an output.

{{tffile "examples/resources/default_tcp_check/example_2.tf"}}

{{ .SchemaMarkdown | trimspace }}
