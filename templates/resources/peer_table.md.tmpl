---
page_title: "haproxy-dataplane_peer_table Resource - HAProxy Data Plane"
subcategory: "Configuration"
description: |-
  Manages HAProxy peer table configuration.
---

# haproxy-dataplane_peer_table (Resource)

Use this resource to manage the peer_table object in HAProxy Data Plane API.

## Example Usage

This baseline example shows the minimum Terraform shape for this resource.
Use it as a starting point and adjust the required fields to match your HAProxy configuration model.

{{tffile "examples/resources/peer_table/example_1.tf"}}

{{ .SchemaMarkdown | trimspace }}
