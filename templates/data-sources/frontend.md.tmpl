---
page_title: "haproxy-dataplane_frontend Data Source - HAProxy Data Plane"
subcategory: "Configuration"
description: |-
  Reads an HAProxy frontend section by name.
---

# haproxy-dataplane_frontend (Data Source)

Use this data source to inspect frontend configuration from existing HAProxy instances.

## Example Usage

Use this to inspect listener/routing mode before attaching new child resources in a separate module.

```terraform
data "haproxy-dataplane_frontend" "public" {
  name = "fe_public"
}

output "frontend_mode" {
  value = data.haproxy-dataplane_frontend.public.spec["mode"]
}
```

This helps avoid hardcoding assumptions in multi-team Terraform estates.

{{ .SchemaMarkdown | trimspace }}
